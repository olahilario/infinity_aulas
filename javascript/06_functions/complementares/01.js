function area(a,h) {
  return a*h
   }
   function processinput(callback) {
      let a = 30
      let h = 40
      return callback(a,h);
   }

  alert(processinput(area));


// d) As funções podem ser chamadas antes de serem declaradas no código.

